//! Java package-path alias for the real sheet handler context.

/// The Java-compatible name resolves to the runtime callback type.
pub type SheetWriteHandlerContext = crate::core::WriteSheetContext;
