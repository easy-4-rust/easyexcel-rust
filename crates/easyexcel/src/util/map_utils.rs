//! Java `MapUtils` 门面重导出。

pub use easyexcel_utils::map_utils::{
    LinkedHashMap, new_hash_map, new_hash_map_with_expected_size, new_linked_hash_map,
    new_linked_hash_map_with_expected_size, new_tree_map, to_string_map,
};
