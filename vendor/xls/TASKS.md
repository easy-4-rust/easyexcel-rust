# xls — remaining tasks

Completed work (the TUI fixes, password-protected workbooks, headless reads,
data-manipulation verbs, `query`, write-side formatting, profiling/discovery,
batch & safety, interop, and the full dynamic-array + `LAMBDA` formula engine)
has been removed from this list — it's recorded in [CHANGELOG.md](CHANGELOG.md)
and git history. What remains is deferred by design:

# stretch (bigger scope)

_(Streaming read for huge files and named-range / Excel-table support have
landed — see [CHANGELOG.md](CHANGELOG.md). Nothing currently queued here.)_